Status: DONE

# Task 05: Progression and Persistence Pass

Goal:

Make the browser prototype feel more like a vertical slice with continuity across attempts.

Prompt:

Work only in the browser version under `C:\development\Project Shadow Shift\Web`.

Add lightweight progression or persistence improvements such as:

- checkpoints
- room restart behavior
- preserved unlocks where appropriate
- cleaner retry flow
- a simple save to local storage if it helps and remains reliable

Keep the experience understandable and not overly complicated.

When complete:

- change the top line to `Status: DONE`
- add a short bullet list under `Completion Notes`

Completion Notes:
- Added lightweight checkpoints across the level and a cleaner respawn flow that restarts you from the last activated shrine instead of the very beginning.
- Added a tiny local storage save for major slice progress, preserving the Dash unlock, weapon upgrade, and last reached checkpoint across attempts.
- Updated start/retry behavior so the prototype now feels more like a vertical slice with continuity rather than a raw full reset every time.
