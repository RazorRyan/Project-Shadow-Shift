Status: DONE

# Task 04: UI and Menu Pass

Goal:

Make the browser prototype feel more like a proper game build instead of a raw debug prototype.

Prompt:

Work only in the browser version under `C:\development\Project Shadow Shift\Web`.

Improve UI and menu presentation:

- make the HUD look cleaner and more intentional
- add a simple title screen or start overlay
- improve the death screen presentation
- add a simple win/clear screen
- keep the UI readable on desktop and smaller screens

Do not overbuild. Keep it lightweight and stylish.

When complete:

- change the top line to `Status: DONE`
- add a short bullet list under `Completion Notes`

Completion Notes:
- Added a proper title/start overlay so the browser build opens like a game instead of dropping straight into play.
- Refined the HUD presentation with a cleaner brand/header treatment, tighter control layout, and improved smaller-screen responsiveness.
- Added a simple clear screen, upgraded the death panel styling, and wired all overlays into the current game state without adding dependencies.
